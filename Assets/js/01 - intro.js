// var y = 0;
// funçao 1o grau: ax + = 0
// ex.: f(1) = a . 1 + b / f(2) = a . 2 + b
// return mostra e permite que eu manipule o valor, enquanto console.log só mostra

function soma (a,b){ // declara/chama a função
  return a+b;       // retorna o resultado da função 
}

soma(3,5);